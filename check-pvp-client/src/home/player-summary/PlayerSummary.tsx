import React, { SFC } from 'react';
import styled from 'styled-components';

const Container = styled.div`
    background-color: #201E21;
    min-height: 100vh;
    margin-top: 20px;
`

const PlayerSummary: SFC<{}> = (props) => {
    return(
        <Container></Container>
    );
};

export default PlayerSummary;